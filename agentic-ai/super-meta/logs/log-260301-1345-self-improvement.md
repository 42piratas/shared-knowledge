# SUPER-META Self-Improvement: 2026-03-01

**Triggered during project:** 42Bros

| # | Target | Before | After | Rationale |
|:--|:--|:--|:--|:--|
| 1 | Context template in `super-meta.md` §Project-Local Context Template | Template had no mechanism for storing project-specific rules — only operational state (run history, watch items, improvement backlog, config) | Added `## Project-Specific Rules` section to template with placeholder entries (git workflow, naming, security, other). Added Session Start sub-bullet: "If `## Project-Specific Rules` section exists → apply those rules for the remainder of the session" | SUPER-META re-derived project rules from scratch every session by reading `principles.md` and `context.md`. No persistent, curated subset of rules that specifically affect SUPER-META's behavior survived across sessions. Rules are stored as references to canonical sources (not duplicated content) to avoid drift. |
